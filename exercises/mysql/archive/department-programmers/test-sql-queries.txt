==============================================================================================================================================
=======================================================Практическая работа 6.=================================================================
==============================================================================================================================================
---------------------------------------------------------------------------------------------------------------------
6.0.1./*Выбирает уникальные значения в столбце city_num из таблицы customers*/

SELECT 
	DISTINCT city_num
FROM 
	customers;
---------------------------------------------------------------------------------------------------------------------
6.0.2./*Выбирает уникальные значения в столбце rating из таблицы customers */

SELECT 
	DISTINCT rating
FROM 
	customers;
---------------------------------------------------------------------------------------------------------------------
6.0.3./*Выбирает уникальные значения в столбце commission_pct из таблицы salespeople*/

SELECT 
	DISTINCT commission_pct 
FROM 
	salespeople;
---------------------------------------------------------------------------------------------------------------------
6.0.4./*Выбирает все поля из таблицы salespeople при условии, что продавец из Лондона*/

SELECT 
	salespeople.sp_num,
	salespeople.sp_name,
	salespeople.commission_pct,
	city.city_name
FROM 
	salespeople,
	city
WHERE 
	city.city_num = salespeople.city_num 
 	AND city.city_name = 'London';
---------------------------------------------------------------------------------------------------------------------
6.0.5./*Выбирает все поля из таблицы salespeople при условии, что комиссия продавца больше 0.12% */

SELECT * 
FROM 
	salespeople
WHERE 
	commission_pct > 0.12;
---------------------------------------------------------------------------------------------------------------------
6.0.6./*Выбирает все поля из таблицы customers при условии, что заказчик из Рима */

SELECT 
	customers.c_num,
	customers.c_name,
	city.city_name,
	customers.rating,
	customers.s_num
FROM 
	customers,
	city
WHERE 
	city.city_num = customers.city_num
	AND city.city_name = 'Rome';
---------------------------------------------------------------------------------------------------------------------
6.0.7./*Выбирает все поля из таблицы customers при условии, что заказчик имеет рейтинг 100 или 300*/

SELECT * 
FROM 
	customers 
WHERE 
	rating = 100 
	OR 
	rating = 300;
---------------------------------------------------------------------------------------------------------------------
6.0.8./*Выбирает все поля из таблицы orders при условии, что стоимость заказа меньше 1000*/

SELECT * 
FROM 
	orders
WHERE 
	amt < 1000;
---------------------------------------------------------------------------------------------------------------------
6.0.9./*Выбирает все поля из таблицы customers при условии, что заказчик из Рима и его рейтинг 200*/

SELECT 
	customers.c_num,
	customers.c_name,
	city.city_name,
	customers.rating,
	customers.sp_num
FROM 
	customers,
    	city
WHERE 
	city.city_num = customers.city_num
    AND city.city_name = 'Rome'
    AND rating = 200;
---------------------------------------------------------------------------------------------------------------------
6.0.10./*Выбирает все поля из таблицы customers при условии, что заказчик из Берлина или его рейтинг 300*/

SELECT 
	customers.c_num,
	customers.c_name,
	city.city_name,
	customers.rating,
	customers.sp_num
FROM 
	customers,
    city
WHERE 
	city.city_num = customers.city_num
    AND (city.city_name = 'Berlin'
   	 OR
	 rating = 300);
================================================================================================================================================
=======================================================Практическая работа 6.1.=================================================================
================================================================================================================================================

6.1.1./*все поля из таблицы Заказчики, в которых заказчиков обслуживали продавцы 1001, 1002 и 1007*/

SELECT * 
FROM 
	customers
WHERE 
	sp_num IN (1001,1002,1007);
---------------------------------------------------------------------------------------------------------------------
6.1.2./*все поля из таблицы Заказы, в которых заказы оформляли заказчики 2001, 2004 и 2008*/

SELECT * 
FROM 
	orders 
WHERE 
	c_num IN (2001, 2004, 2008);
---------------------------------------------------------------------------------------------------------------------
6.1.3./*из таблицы Продавцы все записи, где номер продавца от 1001 до 1005*/

SELECT * 
FROM 
	salespeople 
WHERE 
	sp_num BETWEEN 1001 and 1005;
---------------------------------------------------------------------------------------------------------------------
6.1.4./*из таблицы Продавцы все записи, где коэффициент продавца от 10% до 13%*/

SELECT * 
FROM 
	salespeople
WHERE 
	commission_pct BETWEEN 0.1 AND 0.13;
---------------------------------------------------------------------------------------------------------------------
6.1.5./*выберите из таблицы Заказчики все записи где имя заказчика от буквы А до G*/

SELECT * 
FROM 
	customers
WHERE 
	c_name BETWEEN 'A' AND 'G';
---------------------------------------------------------------------------------------------------------------------
6.1.6./*выберите из таблицы Заказчики все записи где заказчики проживают в городах от буквы А до L */

SELECT * 
FROM 
	customers
WHERE 
	city_num IN (
	(SELECT
		 city_num
	 FROM 
		city 
	 WHERE 
		city_name BETWEEN 'A' AND 'L'
	));
---------------------------------------------------------------------------------------------------------------------
6.1.7./*выберите из таблицы Заказы все заказы, которые были оформлены с 10.03.2021 по 10.05.2021*/

SELECT * 
FROM 
	orders
WHERE 
	o_date BETWEEN '2021-03-10' AND '2021-05-10';
---------------------------------------------------------------------------------------------------------------------
6.1.8./*выведите все данные о заказчиках, имя которых начинается с буквы 'С'*/

SELECT * 
FROM 
	customers
WHERE 
	LIKE 'C%';
---------------------------------------------------------------------------------------------------------------------
6.1.9./*выведите все данные о заказчиках, имя которых начинается с буквы 'G'*/

SELECT * 
FROM 
	customers
WHERE 
	LIKE 'G%';
---------------------------------------------------------------------------------------------------------------------
6.1.10./*выведите все данные о заказчиках, имя которых начинается с буквы 'G' и заканчивается 's'*/

SELECT * 
FROM 
	customers
WHERE 
	LIKE 'G%s';

================================================================================================================================================
=======================================================Практическая работа 7.===================================================================
================================================================================================================================================


7.0.1./*сколько продавцов в таблице Продавцы? Столбец с результатом назовите «Количество продавцов»*/

SELECT 
	COUNT(DISTINCT sp_num) AS «Количество продавцов»
FROM 
	salespeople;
---------------------------------------------------------------------------------------------------------------------
7.0.2./*из скольки городов продавцы? Столбец с результатом назовите «Города продавцов»*/

SELECT 
	COUNT(DISTINCT city_num) AS «Города продавцов»
FROM 
	salespeople;
---------------------------------------------------------------------------------------------------------------------
7.0.3./*сколько строк в таблице Заказчики? Столбец с результатом назовите «Количество строк»*/

SELECT 
	COUNT(*) AS «Количество строк»
FROM 
	customers;
---------------------------------------------------------------------------------------------------------------------
7.0.4./*какой общий рейтинг заказчиков? Столбец с результатом назовите «Общий рейтинг заказчиков»*/

SELECT 
	SUM(rating) AS «Общий рейтинг заказчиков»
FROM 
	customers;
---------------------------------------------------------------------------------------------------------------------
7.0.5./*на какую сумму сделаны все заказы? Столбец с результатом назовите «Сумма заказов» */

SELECT 
	SUM(amt) AS «Сумма заказов»
FROM 
	orders;
---------------------------------------------------------------------------------------------------------------------
7.0.6./*какой средний рейтинг Заказчиков? Столбец с результатом назовите «Средний рейтинг заказчиков»*/

SELECT 
	AVG(rating) AS «Средний рейтинг заказчиков»
FROM 
	customers;
---------------------------------------------------------------------------------------------------------------------
7.0.7./*какова средняя стоимость заказа? Столбец с результатом назовите «Средняя стоимость заказа»*/
SELECT 
	AVG(amt) AS «Средняя стоимость заказа»
FROM 
	orders;
---------------------------------------------------------------------------------------------------------------------
7.0.8./*какой минимальный рейтинг заказчика? Столбец с результатом назовите «Минимальный рейтинг заказчика»*/

SELECT 
	MIN(rating) AS «Минимальный рейтинг заказчика»
FROM 
	customers;
---------------------------------------------------------------------------------------------------------------------
7.0.9./*какой максимальная комиссия продавца? Столбец с результатом назовите «Максимальная комиссия продавца»*/

SELECT 
	MAX(pommission_pct) AS «Максимальная комиссия продавца»
FROM 
	salespeople;
---------------------------------------------------------------------------------------------------------------------
7.0.10./*посчитайте общую стоимость заказа с учётом доставки. Столбец с результатом назовите «Общая стоимость заказа»*/

SELECT 
	(amt + dostavka)  AS «Общая стоимость заказа»
FROM 
	orders;
---------------------------------------------------------------------------------------------------------------------
7.0.11./*выведите из таблицы Заказчики поля: Имя, Город и Рейтинг, уменьшенный в 50 раз*/

SELECT 
	customers.c_name,
	city.city_name,
	customers.rating / 50 AS "rating"
FROM 
	customers, 
	city
WHERE 
	city.city_num = customers.city_num;
---------------------------------------------------------------------------------------------------------------------
7.0.12./*выведите из таблицы Продавцы поля: Имя, Город, синус комиссионных, увеличенный в 10 раз*/

SELECT 
	salespeople.sp_name,
	city.city_name,
	ROUND(SIN (salespeople.commission_pct * 10), 5) AS "commission_pct"
FROM 
	salespeople,
	city
WHERE 
	city.city_num = salespeople.city_num;
---------------------------------------------------------------------------------------------------------------------
7.0.13./*выведите строковую константу, поясняющую поле 'cnum' из таблицы Заказчики, Номер заказчика, Имя заказчика и Город*/

SELECT 
	'Уникальный номер заказчикa',
	customers.c_num,
	customers.c_name,
	city.city_name,
FROM 
	customers,
	city
WHERE 
	city.city_num = customers.city_num;
---------------------------------------------------------------------------------------------------------------------
7.0.14./*выведите строковые константы, поясняющие поля 'snum' и 'comm' из таблицы Продавцы,
 Номер, Имя продавца, Город, Комиссионные*/

SELECT 
	'Уникальный номер продавцa',
	salespeople.sp_num,
	salespeople.sp_name,
	city.city_name,
	'Комиссия продавца в процентах:',
	(salespeople.commission_pct * 100) AS commission_pct,
	'%'
FROM 
	salespeople,
	city
WHERE 
	city.city_num = salespeople.city_num;

================================================================================================================================================
=======================================================Практическая работа 8.===================================================================
================================================================================================================================================

8.0.1./*запрос, который сосчитал бы все суммы приобретений на 3 Октября*/

SELECT 
	COUNT (amt)
FROM 
	orders
WHERE 
	o_date = '2021-03-10';
---------------------------------------------------------------------------------------------------------------------
8.0.2./*запрос, который сосчитал бы число различных, отличных от NULL значений поля city в таблице Заказчиков*/

SELECT 
	COUNT(DISTINCT city_num) AS city
FROM 
	customers
---------------------------------------------------------------------------------------------------------------------
8.0.3./*запрос, который выбрал бы наименьшую сумму для каждого заказчика*/

SELECT 
	c_num,
	MIN (amt) AS "Минимальная сумма заказа"
FROM 
	orders
GROUP BY
	c_num;
---------------------------------------------------------------------------------------------------------------------
8.0.4./*запрос, который выбрал бы наименьшую сумму приобретений получаемую каждым продавцом каждый день*/

SELECT 
	sp_num,
	o_date,
	MIN (amt) AS "Минимальный доход"
FROM 
	orders
GROUP BY
	sp_num, o_date;
---------------------------------------------------------------------------------------------------------------------
8.0.5./*показывает наименьшие Заказы для заказчика Cisneros, Grass и Clemens*/

SELECT 
	c_num,
	MIN (amt) AS "Наименьший заказ"
FROM 
	orders
GROUP BY
	c_num
HAVING 
	c_num IN (
	(SELECT 
		c_num 
	 FROM 
		customers 
	 WHERE 
		c_name IN ('Cisneros','Grass','Clemens')
	));
---------------------------------------------------------------------------------------------------------------------
8.0.6./*запрос, который бы выбирал заказчиков в алфавитном порядке, чьи имена начинаются с буквы G*/

SELECT 
	c_num,
	c_name
FROM 
	customers
WHERE
	c_name LIKE ('G%')
ORDER BY
	c_name ASC;
---------------------------------------------------------------------------------------------------------------------
8.0.7./*запрос, который выбрал бы высшую оценку в каждом городе*/

SELECT 
	city,
	MAX(customers.rating) AS "Высшая оценка"
FROM 
	customers
GROUP BY
	city;
---------------------------------------------------------------------------------------------------------------------
8.0.8./*запрос, который сосчитал бы число Продавцов, регистрирующих свои Заказы, за каждый день 
(если продавец имел более одного Заказа в данный день, он должен учитываться только один раз)*/

SELECT 
	COUNT(DISTINCT sp_num) AS 'Количество продавцов',
	o_date
FROM 
	orders
GROUP BY
	o_date;
---------------------------------------------------------------------------------------------------------------------
8.0.9./*запрос, который сосчитал бы число Заказов за каждый день для каждого Продавца*/

SELECT 
	sp_num,
	o_date,
	COUNT(o_num) AS 'Количество заказов'
FROM 
	orders
GROUP BY
	sp_num, o_date;

================================================================================================================================================
=======================================================Практическая работа 8.1.=================================================================
================================================================================================================================================

8.1.1./*Выведите названия всех заказчиков относительно рейтинга, 
т. е. по каждому рейтингу должны быть выведены те заказчики, которые к нему относятся*/

SELECT 
	rating, c_name
FROM 
	customers
ORDER BY 
	rating DESC;
---------------------------------------------------------------------------------------------------------------------
8.1.2./*Выведите заказчиков, которых обслуживает конкретный продавец,
 т. е. по каждому продавцу должен быть выведен список заказчиков, с которыми он работает*/

SELECT 
	sp_num AS 'Продавец'
	c_num, 
	c_name AS 'Заказчик'
FROM 
	customers 
ORDER BY 
	sp_num ASC;
---------------------------------------------------------------------------------------------------------------------
8.1.3./*Вывести из таблицы Продавцы поля Номер, Имя, Город упорядоченные по убыванию по полю 'sname'.*/

SELECT
	sp_num,
	sp_name,
        city
FROM 
	salespeople
ORDER BY 
	sp_name DESC;
---------------------------------------------------------------------------------------------------------------------
8.1.4./*Вывести из таблицы Заказчики поля Имя, Город, Рейтинг упорядоченные по возрастанию по полям 'rating' и 'cname'.*/

SELECT
	c_name,
    	city,
    	rating
FROM 
	customers
ORDER BY 
	rating ASC, c_name ASC;
---------------------------------------------------------------------------------------------------------------------
8.1.5./*Вывести из таблицы Продавцы поля Имя, Город, сгруппированные по полю Город, и упорядоченные по полю Имя*/

SELECT
	sp_name,
        city
FROM
	salespeople
GROUP BY
	city /*Нельза сгруппировать по полю город*/
ORDER BY 
	sp_name;
---------------------------------------------------------------------------------------------------------------------
8.1.6./*Вывести из таблицы Заказчики поля Имя, Город, Рейтинг, сгруппированные по 2-му столбцу*/

SELECT
	c_name,
    	city,
    	rating
FROM 
	customers
GROUP BY
	city /*Нельза сгруппировать по полю город*/
ORDER BY
	city ASC;
---------------------------------------------------------------------------------------------------------------------
8.1.7./*Вывести из таблицы Заказчики поля Город и количество рейтингов каждого города  упорядоченных по возрастанию*/

SELECT 
	city,
	COUNT(customers.rating) AS 'Количество рейтингов'
FROM 
	customers
GROUP BY
	city
ORDER BY 
	2 ASC;

================================================================================================================================================
=======================================================Практическая работа 9.=================================================================
================================================================================================================================================

9.0.1./*Вывести фамилию продавца, фамилию покупателя и сумму покупки между 700 и 2000*/

SELECT
    salespeople.sp_name,
    customers.c_name,
    orders.amt
FROM
    salespeople,
    customers,
    orders
WHERE
    salespeople.sp_num = orders.sp_num
    AND customers.c_num = orders.c_num
    AND orders.amt BETWEEN 700 AND 2000
ORDER BY
	orders.amt DESC;
---------------------------------------------------------------------------------------------------------------------
9.0.2./*Вывести фамилию продавца и сумму его покупки без комиссии*/

SELECT 
	salespeople.sp_name,
	ROUND(orders.amt - (orders.amt * salespeople.commission_pct)) AS "Сумма покупки без комиссии"
FROM 
	salespeople,
	orders
WHERE
	salespeople.sp_num = orders.sp_num;
---------------------------------------------------------------------------------------------------------------------
9.0.3./*Вывести фамилию продавцов и соответствующих им фамилии покупателей*/

SELECT
	salespeople.sp_name,
        customers.c_name
FROM 
	salespeople,
    	customers
WHERE 
	customers.sp_num = salespeople.sp_num
ORDER BY 
	1 DESC;
---------------------------------------------------------------------------------------------------------------------
9.0.4./*Вывести фамилию продавцов и фамилию покупателей, которые из одного города (поле Название города так же вывести)*/

SELECT
	salespeople.sp_name,
    	customers.c_name,
    	city
FROM 
	salespeople,
	customers
WHERE 
	salespeople.city = customers.city
ORDER BY 
	3 ASC;
---------------------------------------------------------------------------------------------------------------------
9.0.5./*Вывести поле дата, где комиссия продавца меньше 0.12 (поле Комиссия так же вывести)*/

SELECT
	orders.o_date,
        salespeople.commission_pct
FROM
	salespeople,
    	orders
WHERE 
	salespeople.commission_pct < 0.12 
	AND salespeople.sp_num = orders.sp_num
ORDER BY 
	1 ASC;
---------------------------------------------------------------------------------------------------------------------
9.0.6./*Вывести поле Сумма заказа покупателя, у которого рейтинг больше 100 (поле Рейтинг так же вывести)*/

SELECT
	orders.amt,
        customers.rating
FROM
	customers,
    	orders
WHERE 
	customers.rating > 100 AND
	customers.c_num = orders.c_num;
---------------------------------------------------------------------------------------------------------------------
9.0.7./*Вывести имя заказчика, у которого заказ совершен 10.03.2021 (поле Дата так же вывести)*/

SELECT 
	customers.c_name,
	orders.o_date
FROM 
	customers,
	orders
WHERE
	orders.o_date = '2021-03-10' AND
	orders.c_num = customers.c_num
ORDER BY 
	1 DESC;

---------------------------------------------------------------------------------------------------------------------
9.0.8./*Вывести имя покупателя, сумму его заказа и комиссионные продавца за 10.04.2021 (поле Дата так же вывести)*/

SELECT 
	customers.c_name AS 'Имя покупателя',
	orders.amt AS 'Сумма заказа',
	salespeople.commission_pct AS 'Комисионные продавца',
	orders.o_date
FROM 
	customers,
	orders,
 	salespeople
WHERE
	orders.o_date = '2021-04-10' 
	AND orders.c_num = customers.c_num
	AND orders.sp_num = salespeople.sp_num 
	
ORDER BY 
	2 DESC;
---------------------------------------------------------------------------------------------------------------------
9.0.9./*Вывести имя и рейтинг заказчиков, у которых сумма заказа больше 3000 и покупка совершена 10.03.2021 
(поля Сумма заказа и Дата так же вывести)*/

SELECT
	customers.c_name,
        customers.rating,
	orders.amt,
	orders.o_date
FROM
	customers,
    	orders
WHERE 
	customers.c_num = orders.c_num
	AND orders.amt > 3000 
	AND orders.o_date = '2021-03-10'
---------------------------------------------------------------------------------------------------------------------
9.0.10./*Вывести номера заказов, продавцы и заказчики которых живут в Сан Джосе 
(поля ФИО заказчика, ФИО продавца и Название города так же вывести)*/

SELECT 
	orders.o_num,
	salespeople.sp_name,
	customers.c_name,
	salespeople.city
FROM 
	customers,
	orders,
	salespeople
WHERE
	salespeople.city = 'San Jose' 
	AND orders.sp_num = salespeople.sp_num 
	AND orders.c_num = customers.c_num 
	AND customers.city = salespeople.city


================================================================================================================================================
=======================================================Практическая работа 10.=================================================================
================================================================================================================================================

10.0.1./*Напишите запрос, который бы вывел все пары продавцов, живущих в одном и том же городе. 
Исключите комбинации продавцов с ними же, а также дубликаты строк, выводимых в обратном порядке.*/

SELECT 
	sp1.sp_name
      ,	sp2.sp_name
      ,	sp1.city
FROM 
	salespeople AS sp1 
      ,	salespeople AS sp2
WHERE 
	  sp1.city = sp2.city
      AND sp1.sp_num < sp2.sp_num;
      
---------------------------------------------------------------------------------------------------------------------
10.0.2./*Напишите запрос, который бы вывел все пары заказчиков, с одним и тем же рейтингом. 
Исключите комбинации заказчиков с ними же, а также дубликаты строк, выводимых в обратном порядке.*/

SELECT 
	c1.c_name
      , c2.c_name
      ,	c1.rating
FROM 
	customers AS c1
      , customers AS c2
WHERE 
	  c1.rating = c2.rating
      AND c1.c_name < c2.c_name
ORDER BY
	c1.rating;
      
---------------------------------------------------------------------------------------------------------------------
10.0.3./*Напишите запрос, который вывел бы все пары Заказов (onum) по данным заказчикам,
 имена этих заказчиков, и исключал дубликаты из вывода, как в предыдущем вопросе*/

SELECT 
	c.c_name
      ,	o1.o_num
      ,	o2.o_num
	
FROM 
	customers AS c
      ,	orders AS o1
      , orders AS o2
WHERE 
	  o1.c_num = o2.c_num
      AND o1.c_num = c.c_num
      AND o2.c_num = c.c_num
      AND o1.o_num > o2.o_num;
      
---------------------------------------------------------------------------------------------------------------------
10.0.4./*Напишите запрос, который вывел бы имена (cname) и города (city) всех заказчиков с одинаковым рейтингом (rating).
 Исключите комбинации заказчиков с ними же, а также дубликаты строк, выводимых в обратном порядке.*/

SELECT
	c1.c_name
      , c1.city
      , c2.c_name
      ,	c2.city
      , c1.rating
FROM 
	customers AS c1
      ,	customers AS c2
WHERE 
	c1.rating = c2.rating
    AND c1.c_num > c2.c_num;

---------------------------------------------------------------------------------------------------------------------
10.0.5./*Напишите запрос, который вывел бы имена (cname) и города (city) всех заказчиков с такой же оценкой (rating) как у Hoffman. 
Напишите запрос, использующий поле cnum Hoffman, а не его оценку, так, чтобы оно могло быть использовано, если его оценка вдруг изменится.*/

SELECT
	c1.c_name
      , c1.city
      , c2.c_name
      ,	c2.city
      , c1.rating
FROM 
	customers AS c1
      ,	customers AS c2
WHERE 
	c1.c_num = 2001
    OR  c2.c_num = 2001
    AND c1.c_num > c2.c_num;
---------------------------------------------------------------------------------------------------------------------
10.0.6./*Напишите запрос, который вывел бы дату(odate) и сумму заказа (amt) у одинаковых заказчиков.
 Исключите комбинации заказчиков с ними же, а также дубликаты строк, выводимых в обратном порядке.*/

SELECT
	orders.amt,
        customers.rating
FROM
	customers,
    	orders
WHERE 
	customers.rating > 100 AND
	customers.c_num = orders.c_num;
---------------------------------------------------------------------------------------------------------------------
10.0.7./*Напишите запрос, который вывел бы все номера заказчиков (cnum),
 которые сделали заказы 10.04.2021 и 10.05.2021*/

SELECT 
	customers.c_name,
	orders.o_date
FROM 
	customers,
	orders
WHERE
	orders.o_date = '2021-03-10' AND
	orders.c_num = customers.c_num
ORDER BY 
	1 DESC;

---------------------------------------------------------------------------------------------------------------------
10.0.8./*Напишите запрос, который вывел бы все имена заказчиков (cname), которые сделали заказы у Peel и Serres*/

SELECT 
	customers.c_name AS 'Имя покупателя',
	orders.amt AS 'Сумма заказа',
	salespeople.commission_pct AS 'Комисионные продавца',
	orders.o_date
FROM 
	customers,
	orders,
 	salespeople
WHERE
	orders.o_date = '2021-04-10' 
	AND orders.c_num = customers.c_num
	AND orders.sp_num = salespeople.sp_num 
	
ORDER BY 
	2 DESC;
---------------------------------------------------------------------------------------------------------------------
10.0.9./*Напишите запрос, который вывел бы все пары сумм (amt), сделанные 10/05/2021 и 10/06/2021*/

SELECT
	customers.c_name,
        customers.rating,
	orders.amt,
	orders.o_date
FROM
	customers,
    	orders
WHERE 
	customers.c_num = orders.c_num
	AND orders.amt > 3000 
	AND orders.o_date = '2021-03-10'
---------------------------------------------------------------------------------------------------------------------
10.0.10./*Напишите запрос, который бы вывел все пары продавцов, с одинаковой комиссией. 
Исключите комбинации продавцов с ними же, а также дубликаты строк, выводимых в обратном порядке.
*/

SELECT 
	orders.o_num,
	salespeople.sp_name,
	customers.c_name,
	city.city_name
FROM 
	customers,
	orders,
	salespeople,
	city
WHERE
	city.city_num = customers.city_num 
	AND city.city_num = salespeople.city_num
	AND city.city_name = 'San Jose' 
	AND orders.sp_num = salespeople.sp_num 
	AND orders.c_num = customers.c_num 
	AND customers.city_num = salespeople.city_num 



================================================================================================================================================
=======================================================Практическая работа 11.=================================================================
================================================================================================================================================
11.0.1.
SELECT *
FROM 
	orders
WHERE 
	c_num = (SELECT 
			c_num
		 FROM
			orders
		 WHERE c_name= 'Hoffman');
      
---------------------------------------------------------------------------------------------------------------------
11.0.2.
SELECT 
	o_num
FROM 
	orders
WHERE 
	 sp_num = (SELECT 
			sp_num
		 FROM
			salespeople
		 WHERE city = 'New York');
      
---------------------------------------------------------------------------------------------------------------------
11.0.3.
SELECT 
	c_name
FROM 
	customers
WHERE 
	 sp_num = (SELECT 
			sp_num
		 FROM
			salespeople
		 WHERE 
			sp_name = 'Motika');
---------------------------------------------------------------------------------------------------------------------
11.0.4.
SELECT 
	sp_num
FROM 
	salespeople
WHERE 
	 sp_num = (SELECT 
			sp_num
		 FROM
			customers
		 WHERE 
			c_name = 'Liu');
---------------------------------------------------------------------------------------------------------------------
11.0.5.
SELECT 
	o_num
FROM 
	orders
WHERE 
	 c_num = (SELECT 
			c_num
		 FROM
			customers
		 WHERE 
			city = 'Rome' AND
			rating = 100);
);


================================================================================================================================================
=======================================================Практическая работа 12.=================================================================
================================================================================================================================================
12.0.1.
SELECT 
    	c.c_num,
    	c.c_name AS "Имена заказчиков",
    	c.rating,
    	TRUNCATE(AVG(o.amt), 2) AS avg_amt
FROM 
	customers c,
    	orders o
WHERE
	c.c_num = o.c_num
GROUP BY 
	c.c_name
---------------------------------------------------------------------------------------------------------------------
12.0.2.
SELECT 
	c_num,
	c_name AS "Имена заказчиков",
    	rating
FROM 
	customers
WHERE
	city = 'Rome' AND
    	rating < (SELECT 
              		AVG(rating)
              	  FROM
              		customers)
---------------------------------------------------------------------------------------------------------------------
12.0.3.
SELECT 
	salespeople.sp_num,
	salespeople.sp_name,
    	salespeople.commission_pct
FROM 
	salespeople
WHERE
    	sp_num IN(SELECT 
              		customers.sp_num
              	  FROM
              		customers
              	  WHERE
             		customers.city = 'Paris')
---------------------------------------------------------------------------------------------------------------------
12.0.4. /*Написать запрос, который подсчитывает количество Заказов  с суммой приобретения ниже среднего, 
совершенных 10/03/2021.*/

SELECT 
	COUNT(orders.o_num) AS count_orders
FROM 
    	orders
WHERE
	o_date LIKE '____-03-10' AND
    	amt < (SELECT 
              		AVG(amt)
               FROM
              		orders)
---------------------------------------------------------------------------------------------------------------------
12.0.5. /*Написать запрос, который выводит Продавца, уникальный номер которого на 1000 меньше уникального номера Заказчика, 
проживающего в Берлине.*/

SELECT 
	*
FROM 
    	salespeople
WHERE
    	sp_num = (SELECT 
              	     c_num - 1000
              	 FROM
              	     customers
             	 WHERE
		     city = 'Berlin')

---------------------------------------------------------------------------------------------------------------------
12.0.6. /*Напишите запрос, который бы выбрал общую сумму всех приобретений в Заказах для каждого продавца,
 у которого эта общая сумма больше, чем сумма наибольшего Заказа в таблице*/

SELECT 
	sp_num,
	SUM(amt) AS sum_amt
FROM 
    	orders
GROUP BY
	sp_num
HAVING
    	sum_amt > (SELECT 
              		MAX(amt)
                  FROM
              		orders)


================================================================================================================================================
=======================================================Практическая работа 13.=================================================================
================================================================================================================================================
13.0.1. /*Создайте объединение из двух запросов, которое показало бы имена, города, и оценки всех заказчиков. 
Те из них, которые имеют поле rating=200 и более, должны, кроме того, иметь слова "Высокий Рейтинг", 
а остальные должны иметь слова "Низкий Рейтинг"*/
SELECT
	c_name, 
    	city, 
    	rating, 
    	"Высокий рейтинг"
FROM 
	customers
WHERE 
	rating >= 200
UNION
SELECT
	c_name, 
    	city, 
    	rating, 
    	"Низкий Рейтинг"
FROM 
	customers
WHERE 
	rating < 200
ORDER BY
	3 DESC
---------------------------------------------------------------------------------------------------------------------
13.0.2./*Напишите команду, которая бы вывела имена и номера каждого продавца и каждого заказчика, 
которые имеют больше одного заказа. Результат представьте в алфавитном порядке*/

SELECT
	sp.sp_name,
    	sp.sp_num,
	COUNT(o1.sp_num) AS "Количество заказов"
FROM 
	salespeople sp,
    	orders o1
WHERE 
     sp.sp_num = o1.sp_num AND
     sp.sp_num IN(SELECT o2.sp_num 
		      FROM orders o2 
		      GROUP BY o2.sp_num 
		      HAVING COUNT(o2.o_num) > 1)
GROUP BY o1.sp_num

UNION
SELECT
	c.c_name, 
  	c.c_num,
        COUNT(o3.c_num) AS "Количество заказов"
FROM 
	customers c,
        orders o3 
WHERE 
	c.c_num = o3.c_num AND
	c.c_num IN(SELECT o4.c_num 
		   FROM orders o4
		   GROUP BY o4.c_num 
		   HAVING COUNT(o4.o_num) > 1)
GROUP BY o3.c_num
ORDER BY 1 ASC, 2 ASC
---------------------------------------------------------------------------------------------------------------------
13.0.3./*Сформируйте объединение из трех запросов. 
-первый выбирает поля spnum всех продавцов в San Jose; 
-второй, поля cnum всех заказчиков в San Jose; 
-третий поля onum всех Заказов на 3 Октября. 
Сохраните дубликаты между последними двумя запросами, но устраните любую избыточность вывода между каждым из них и самым первым. 
Примечание: в данных типовых таблицах не содержится никакой избыточности. */

SELECT 
      sp_num
    , city
FROM salespeople
WHERE city = 'San Jose'

UNION
SELECT 
      c_num
    , city
FROM customers 
WHERE city = 'San Jose'

UNION ALL
SELECT 
      o_num
    , o_date
FROM orders
WHERE o_date = '2021-03-10'
ORDER BY 1 ASC;


==============================================================================================================================================
=======================================================Практическая работа ?.=================================================================
==============================================================================================================================================
?.0.1. /**/

SELECT * 
FROM 
	salespeople 
WHERE 
	city = ANY(SELECT
               		city  
                   FROM
               		customers);
---------------------------------------------------------------------------------------------------------------------
?.0.2. /**/

SELECT * 
FROM 
	salespeople 
WHERE 
	commission_pct > ALL(SELECT
               			commission_pct  
               		     FROM
               			salespeople
                	     WHERE
                    		city = 'London');
---------------------------------------------------------------------------------------------------------------------
?.0.3. /**/

SELECT * 
FROM 
	orders 
WHERE 
	amt < ALL(SELECT
               		amt  
               	  FROM
               		orders
                  WHERE
                    	o_date = '2021-04-10')
---------------------------------------------------------------------------------------------------------------------
?.0.4. /**/

SELECT 
	sp_num,
    	SUM(amt) AS sum_amt
FROM 
	orders
GROUP BY 
	sp_num
HAVING
	SUM(amt) >= ALL(SELECT
               			SUM(amt)  
               		FROM
               			orders
                	GROUP BY
                    		sp_num)
---------------------------------------------------------------------------------------------------------------------
?.0.5. /**/

SELECT 
	sp.sp_num,
    	sp.sp_name,
    	SUM(o1.amt) AS sum_amt
FROM 
	orders o1,
    	salespeople sp
WHERE
	sp.sp_num = o1.sp_num
GROUP BY 
	sp.sp_num
HAVING
	SUM(o1.amt) >= ALL(SELECT
               			SUM(o2.amt)  
               		   FROM
               			orders o2
                	   GROUP BY
                    		o2.sp_num);

---------------------------------------------------------------------------------------------------------------------

================================================================================================================================================
=======================================================Практическая работа WindowFunctions.=====================================================
================================================================================================================================================
WindowFunctions.1.0. /*Показать самые большие комиссионные продавцов в каждом город, не исключая вывода исходных данных о имени продавца, 
его исходных комиссионных и городе его проживания. Столбец с результатом назовите 'Максимальная комиссия в каждом городе'. 
Пронумеруйте уникальность строк, для результата используйте столбец 'Номер уникальности', 
который разместите перед столбцом со значением максимальной комиссии.*/

SELECT 
    sp_name 
  , commission_pct
  , city
  , ROW_NUMBER() OVER() AS 'Номер уникальности'
  , MAX(commission_pct) OVER(PARTITION BY city) AS 'Максимальная комиссия в каждом городе'
FROM 
    salespeople
---------------------------------------------------------------------------------------------------------------------
WindowFunctions.2.0. /*Покажите суммарный накопительный итог всех оформленных заказов для каждого продавца. 
В выводе должны участвовать столбцы: дата оформления заказа, стоимость заказа, имя(!!!) продавца, 
'Накопительная сумма стоимости заказов'.*/

SELECT 
    o_date
  , amt
  , sp.sp_name
  , SUM(amt) OVER(PARTITION BY sp.sp_name ORDER BY amt) AS 'Накопительная сумма стоимости заказов'
FROM 
    salespeople sp
  , orders
WHERE
    sp.sp_num = orders.sp_num
ORDER BY 
    sp_name
  , amt
---------------------------------------------------------------------------------------------------------------------
WindowFunctions.3.0. /*Показать максимальную стоимость заказа для каждого заказчика и заказ, стоимость которого предшествовала максимальному. 
В выводе должны участвовать столбцы: имя(!!!) продавца, стоимость заказа, 'Максимальная стоимость заказа для каждого заказчика',
 'Стоимость заказа, предшествующая максимальной для данного Заказчика'.*/

WITH 
count_supplement_specialalization AS(
    SELECT
    	   supplement_specialization,
    		COUNT(employee_id) AS number_supplement_specialization
	FROM employees
    WHERE supplement_specialization IS NOT NULL
	GROUP BY supplement_specialization),
    
count_specialization AS(  
	SELECT 
    	specialization,
    	COUNT(employee_id) AS number_specialization
	FROM employees
    WHERE specialization IS NOT NULL
	GROUP BY specialization),
    
joined_queries AS(    
SELECT
	DISTINCT s.specialization,
    NVL(number_specialization, 0) AS number_specializations,
    NVL(number_supplement_specialization, 0) AS number_supplement_specializations
FROM specializations AS s
LEFT OUTER JOIN count_supplement_specialalization AS csp ON(csp.supplement_specialization = s.specialization)
LEFT OUTER JOIN count_specialization AS cp ON(cp.specialization = s.specialization))


SELECT
	specialization,
    number_supplement_specializations + number_specializations AS 'Количество работников'
FROM joined_queries
GROUP BY specialization



-- Version 1.2
WITH
pre_max_amt AS(
   SELECT
       amt
      ,	c_num
      ,	MIN(amt) OVER(PARTITION BY c_num ORDER BY amt) AS pre_max 
   FROM
       orders)
   
SELECT 
    sp.sp_name
  , o.amt
  , o.c_num
  , MAX(o.amt) OVER(PARTITION BY o.c_num) AS 'Максимальная стоимость заказа для каждого заказчика'
  , pre_max AS 'Стоимость заказа, предшествующая максимальной для данного Заказчика'
FROM 
    salespeople AS sp
  , orders AS o
  , pre_max_amt AS pma
WHERE
    sp.sp_num = o.sp_num
    AND pma.c_num = o.c_num
    AND pma.amt = o.amt

---------------------------------------------------------------------------------------------------------------------
WindowFunctions.4.0. /*Показать максимальную суммарную стоимость заказа Заказчиков 
(т.е по каждому заказчику найти его суммарную стоимость заказов и среди этих суммарных стоимостей найти максимальную).*/

WITH 
-- Define CTE query, which return sum of orders and information on customers
sum_amt AS(
	SELECT
    	    customers.c_num
    	  , SUM(amt) OVER(PARTITION BY orders.c_num) AS sum_orders
	FROM 
    	    orders
    	  , customers
	WHERE 
     	    orders.c_num = customers.c_num
	ORDER BY
     	    sum_orders DESC)


SELECT
    c_num
  , MAX(DISTINCT sum_amt.sum_orders) AS max_amt
FROM
    sum_amt;
---------------------------------------------------------------------------------------------------------------------
WindowFunctions.4.1. /*Показать имя Заказчика у которого оказалась эта максимальная суммарная стоимость заказов*/

WITH 
-- Define CTE query, which return sum of orders and information on customers
sum_amt AS(
	SELECT
    	    c.c_num
    	  , c.c_name
    	  , SUM(o.amt) OVER(PARTITION BY o.c_num) AS sum_orders
	FROM 
    	    orders AS o
    	  , customers AS c
	WHERE 
    	    o.c_num = c.c_num
	ORDER BY
    	    sum_orders DESC)


SELECT
    c_num
  , c_name
  , MAX(DISTINCT sum_amt.sum_orders) AS max_amt
FROM
    sum_amt;
---------------------------------------------------------------------------------------------------------------------
WindowFunctions.4.2. /*Показать имя Заказчика который на втором месте по максимальной суммарной стоимости заказов. 
Т.е. дополнить информацию по запросу 4.1 – к выведенной информации по лидеру, добавить информацию по второму месту.*/

WITH 
-- Define CTE query, which return sum of orders and information on customers
rating_sum_amt AS (
	SELECT 
    	    ROW_NUMBER() OVER(ORDER BY sum_orders DESC) AS row_number
    	  , c_num
    	  , c_name
    	  , sum_orders
	FROM (    
	      SELECT
    	    	  DISTINCT c.c_num
    	  	, c.c_name
    	  	, SUM(amt) OVER(PARTITION BY o.c_num) AS sum_orders
	     FROM 
    	    	orders AS o
    	      , customers AS c
	     WHERE 
    	    	o.c_num = c.c_num
	     ORDER BY
    	    	sum_orders DESC) sum_amt
)

SELECT
    c_num
  , c_name
  , sum_orders
FROM
    rating_sum_amt
WHERE 
    row_number BETWEEN 1 AND 2



================================================================================================================================================
=======================================================Практическая работа 14.=================================================================
================================================================================================================================================

14.0.1./*Создайте представление, которое бы показывало всех заказчиков с рейтингом 300*/

CREATE VIEW 14_0_1 AS
SELECT 
     c_name
   , rating
FROM 
     customers
WHERE 
     rating = 300;
      
---------------------------------------------------------------------------------------------------------------------
14.0.2./*Создайте представление, которое бы показывало заказчиков с именем на букву 'C'*/

CREATE VIEW 14_0_2 AS
SELECT 
	c_name
FROM 
	customers 
WHERE 
	c_name LIKE 'C%';
      
---------------------------------------------------------------------------------------------------------------------
14.0.3./*Создайте представление, которое бы показывало всех продавцов из Лондона*/

CREATE VIEW 14_0_3 AS
SELECT 
	sp_name
FROM 
	salespeople
WHERE 
	city = 'London'
---------------------------------------------------------------------------------------------------------------------
14.0.4./*Создайте представление, которое бы показывало все заказы больше 1000 руб.*/

CREATE VIEW 14_0_4 AS
SELECT
	o_num
      , amt
FROM 
	orders
WHERE 
	amt > 1000;
---------------------------------------------------------------------------------------------------------------------
14.0.5./*Создайте представление, которое бы показывало все заказы, которые были сделаны с марта по апрель месяц*/

CREATE VIEW 14_0_5 AS
SELECT
	o_num
      , amt
      , o_date
FROM 
	orders
WHERE 
	o_date LIKE '____-03-__'
     OR o_date LIKE '____-04-__';
---------------------------------------------------------------------------------------------------------------------
14.0.6./*Создайте представление, которое бы показывало всех заказчиков которые имеют самые высокие оценки*/

CREATE VIEW 14_0_6 AS
SELECT
     	  c_name
    	, rating
FROM
	customers
WHERE 
	rating = (SELECT MAX(rating) 
		  FROM customers);

---------------------------------------------------------------------------------------------------------------------
14.0.7./*Создайте представление, которое показывало продавца, заказчика, среднее по заказам, а так же самый дорогой заказ.*/

CREATE VIEW 14_0_7 AS
SELECT 
	c_num
      , sp_num
      , TRUNCATE(AVG(amt), 2) AS 'Среднее по заказам'
      ,	MAX(amt) AS 'Самый дорогой заказ'
FROM 
	orders
GROUP BY 
	sp_num;

---------------------------------------------------------------------------------------------------------------------
14.0.8./*Создайте представление, которое бы показывало количество продавцов в каждом городе.*/

CREATE VIEW 14_0_8 AS
SELECT 
	city
      , COUNT(sp_num) AS numberOfSalespeople
FROM 
 	salespeople
GROUP BY
	city;

---------------------------------------------------------------------------------------------------------------------
14.0.9./*Создайте представление, которое показывало бы коммисионные продавца, его фамилию,
 которые обслуживали заказчиков из Рима и Сан Хосе*/

CREATE VIEW 14_0_9 AS
SELECT
	sp_num
      , sp_name
      ,	commission_pct
FROM
	salespeople AS sp
WHERE 
	sp.sp_num = ANY(SELECT c.sp_num 
			FROM customers AS c 
			WHERE c.city IN('Rome', 'San Jose');

---------------------------------------------------------------------------------------------------------------------
14.0.10./*Создайте представление, которое бы показывало усредненный и общий Заказы для каждого продавца после его имени.
 Предполагается, что все имена — уникальны.*/

CREATE VIEW 14_0_10 AS
SELECT 
        sp.sp_name
      ,	TRUNCATE(AVG(o.amt), 2) AS 'Среднее по заказам'
      , SUM(o.amt) AS 'Общее по заказам'
FROM 
	orders AS o
      ,	salespeople AS sp
WHERE
	sp.sp_num = o.sp_num
GROUP BY 
	sp.sp_name;

-----------------------------------------------------------------------------------------------------------------------
14.0.11./*Создайте представление, которое показывает все заказы у тех продавцов, которые живут в Лондоне*/

CREATE VIEW 14_0_11 AS
SELECT 
	o_num
      , amt
      , sp.sp_num
FROM 
	orders
      , salespeople AS sp
WHERE
	sp.sp_num = orders.sp_num
	AND sp.city = 'London'
ORDER BY
	2 ASC;
---------------------------------------------------------------------------------------------------------------------
14.0.12./*Создайте представление, которое бы показывало каждого продавца с многочисленными заказчиками*/

CREATE VIEW 14_0_12 AS
SELECT 
	sp.sp_num
      , sp.sp_name 
      , COUNT(orders.c_num) AS numberOfCustomers
FROM 
	salespeople AS sp
      , orders
WHERE 
        sp.sp_num = orders.sp_num
GROUP BY 
	sp.sp_num
HAVING COUNT(c_num) > 1;
---------------------------------------------------------------------------------------------------------------------
14.0.13./*Создайте представление, которое покажет всех продавцов, которые делали заказы в апреле.*/

CREATE VIEW 14_0_13 AS
SELECT 
	sp.sp_num
      , sp.sp_name
      , o.o_date
FROM 
	salespeople AS sp
      , orders AS o
WHERE 
	  sp.sp_num = o.sp_num
      AND o.o_date LIKE '____-04-__'
GROUP BY
	sp.sp_num



================================================================================================================================================
=======================================================Практическая работа 18.=================================================================
================================================================================================================================================

18.0.1./**/

mysql -uroot –p

CREATEUSER ‘gruber_user’@'localhost' IDENTIFIEDBY '12345';
      
---------------------------------------------------------------------------------------------------------------------
18.0.2./**/

mysql -uroot –p

GRANT SELECT PRIVILEGES ON paramonov.orders TO 'gruber_user'@'localhost'; 
GRANT SELECT PRIVILEGES ON paramonov.salespeople TO 'gruber_user'@'localhost';
FLUSH PRIVILEGES;

      
---------------------------------------------------------------------------------------------------------------------
18.0.3./**/

mysql -u gruber_user-p
use paramonov;

SELECT *
FROM salespeople
ORDER BY sp_name DESC;

---------------------------------------------------------------------------------------------------------------------
18.0.4./**/

mysql -uroot –p

GRANT INSERT PRIVILEGES ON paramonov.orders TO 'gruber_user'@'localhost'; 
FLUSH PRIVILEGES;
---------------------------------------------------------------------------------------------------------------------
18.0.5./**/

mysql -u gruber_user-p
use paramonov;

INSERT INTO orders (o_num, amt, o_date, c_num, s_num)
VALUES (3012, 2210.23, 2001, 1004);
---------------------------------------------------------------------------------------------------------------------
18.0.6./**/

mysql -uroot –p

REVOKE SELECT ON paramonov.orders FROM 'gruber_user'@'localhost';
FLUSH PRIVILEGES;

---------------------------------------------------------------------------------------------------------------------
18.0.7./**/

mysql -uroot –p

GRAND UPDATE ON paramonov.customers FROM 'gruber_user'@'localhost';
FLUSH PRIVILEGES;

---------------------------------------------------------------------------------------------------------------------
18.0.8./**/

mysql -uroot –p

REVOKE UPDATE ON paramonov.orders FROM 'gruber_user'@'localhost';
FLUSH PRIVILEGES;

---------------------------------------------------------------------------------------------------------------------
18.0.9./**/

mysql -uroot –p

DROP USER 'gruber_user'@'localhost';

================================================================================================================================================
=======================================================Практическая работа JOINs.=================================================================
================================================================================================================================================
JOIN.0.1.
SELECT 
      o_num
    , amt
    , o_date
    , o.c_num
    , o.sp_num
    , c_name
FROM orders o
LEFT OUTER JOIN customers c ON(c.c_num = o.c_num)
WHERE c_name = "Hoffman"
      
---------------------------------------------------------------------------------------------------------------------
JOIN.0.2.
SELECT 
      o_num
    , amt
    , o_date
    , o.c_num
    , o.sp_num
    , sp_name
    , city
FROM orders o
LEFT OUTER JOIN salespeople sp ON(sp.sp_num = o.sp_num)
WHERE city = "New York"
      
---------------------------------------------------------------------------------------------------------------------
JOIN.0.3.
SELECT 
      c_num
    , c_name
    , c.sp_num
    , c.city
    , sp_name
    , sp.city
FROM customers c
LEFT OUTER JOIN salespeople sp ON(sp.sp_num = c.sp_num)
WHERE sp_name = "Motika"
---------------------------------------------------------------------------------------------------------------------
JOIN.0.4.
SELECT 
      sp.sp_num
    , sp_name
    , sp.city
    , c_name
    , c.city
FROM customers c
RIGHT OUTER JOIN salespeople sp ON(sp.sp_num = c.sp_num)
WHERE c_name = "Liu"
---------------------------------------------------------------------------------------------------------------------
JOIN.0.5.
SELECT 
     c.c_num
    , c_name
    , c.city
    , o_num
    , amt
    , o_date
    , o.sp_num
FROM orders o
RIGHT OUTER JOIN customers c ON(o.c_num = c.c_num)
WHERE city = "Rome" AND
	  rating = 100

































