# Task

Using the `clinic` schema, return every patient together with the number of cases they have.

Requirements:

- show patients even when they have zero cases
- return `patient_id`, `full_name`, and `case_count`
- sort by `case_count` descending, then by `full_name`
